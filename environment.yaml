# conda env create -f environment.yaml
name: numgradpy
channels:
  - defaults
  - conda-forge
dependencies:
  - black
  - coverage
  - numpy
  - pre-commit
  - pylint
  - pytest
  - tox
  - pip:
      - covdefaults
